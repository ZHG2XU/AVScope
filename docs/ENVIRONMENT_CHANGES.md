# 环境与新增安装记录

## 2026-08-12

- 本轮未安装新的工具、运行库或依赖文件。
- 继续使用现有 `E:\DevelopmentEnvironment\python\python.exe`。
- 继续使用现有 `E:\DevelopmentEnvironment\ffmpeg-8.1-essentials_build\bin\ffmpeg.exe` 和 `ffprobe.exe`。
- 继续使用现有 `E:\AVScopeTools` 下的 PyInstaller 与 NSIS。
- 项目、临时文件、构建产物和样例报告均放置在 `G:\AVScope`。
